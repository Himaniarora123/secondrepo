pipeline {
    agent any
    stages {
        stage('stack-creation') {
            steps {
                sh "aws cloudformation create-stack --stack-name myfirststack --bucket-name  demobucket456 --template-body file://demo.json --region 'us-east-1'"
            }
        }
    }
}
